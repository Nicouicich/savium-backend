# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Test Database
MONGODB_URI=mongodb://localhost:27017/savium_test

# Test Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=1

# Test JWT Secrets
JWT_ACCESS_SECRET=test-access-secret-key-for-testing-only
JWT_REFRESH_SECRET=test-refresh-secret-key-for-testing-only
JWT_ACCESS_EXPIRES_IN=15d
JWT_REFRESH_EXPIRES_IN=30d

# Test Email Configuration (disabled for testing)
EMAIL_HOST=localhost
EMAIL_PORT=1025
EMAIL_USER=test@savium.com
EMAIL_PASSWORD=testpassword
EMAIL_FROM=noreply@savium-test.com

# Disable external integrations for testing
OPENAI_API_KEY=test-key
WHATSAPP_ACCESS_TOKEN=test-token
TELEGRAM_BOT_TOKEN=test-token

# Test specific settings
DISABLE_EXTERNAL_SERVICES=true
MOCK_EMAIL_SERVICE=true